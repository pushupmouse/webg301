{% form_theme originForm 'bootstrap_5_layout.html.twig' %}
{% extends "base.html.twig" %}

{% block body %}
<div class="container col-md-8 text-center mt-3">
    <div class="card p-3 bg-light">
    <h3 class="alert alert-primary">Edit Origin</h3>
    {{ form_start(originForm) }}

    {{ form_widget(originForm) }}
    <input class="btn btn-outline-danger" type="submit" value="Edit">
    
    {{ form_end(originForm) }}
    </div>
</div>
{% endblock %}